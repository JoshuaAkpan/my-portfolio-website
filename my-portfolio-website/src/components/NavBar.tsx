import Image from "next/image";

export default function NavBar() {
  return (
    <main className="flex items-center justify-between text-[#fafafa] h-20 px-16 text-[20px] font-sans font-semibold">
      <code>Josh</code>
    </main>
  );
}
